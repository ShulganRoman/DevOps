server {
        listen 80 default_server;
        listen [::]:80 default_server;

        location / {
                root /www;
                index index.html;
        }

        location = /404.html {
                internal;
        }
}
